package assessment.java.b;

public class ScoresClump {
    /**
     * <p>
     * Description:
     * Given an array of scores sorted in increasing order, return True if
     * the array contains 3 adjacent scores that differ from each other by
     * at most 2. In other words, if any consecutive triplet of scores has a
     * maximum difference (between the highest and lowest) of 2 or less,
     * the function returns True.
     * </p>
     * <p>
     * Examples:
     * <ul>
     *   <li>scoresClump([3, 4, 5]) -> true</li>
     *   <li>scoresClump([3, 4, 6]) -> false</li>
     *   <li>scoresClump([1, 3, 5, 5]) -> true</li>
     * </ul>
     * </p>
     *
     * @param scores A list of integer scores, sorted in increasing order
     * @return true if there exists a triplet of adjacent scores with a max
     *         difference of 2 or less, false otherwise
     */
    public static boolean scoresClump(int[] scores) {
        // TODO: Implement your solution here
        return false;
    }
}
