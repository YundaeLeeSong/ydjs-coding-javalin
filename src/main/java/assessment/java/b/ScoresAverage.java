package assessment.java.b;

public class ScoresAverage {
    /**
     * <p>
     * Description:
     * Given an array of scores, compute the integer average of the first
     * half and the second half, and return whichever is larger. The second
     * half begins at index len(scores)/2. You must use a helper function
     * that computes the average of the values between two indices.
     * </p>
     * <p>
     * Examples:
     * <ul>
     *   <li>scoresAverage([2, 2, 4, 4]) -> 4</li>
     *   <li>scoresAverage([4, 4, 4, 2, 2, 2]) -> 4</li>
     *   <li>scoresAverage([3, 4, 5, 1, 2, 3]) -> 4</li>
     * </ul>
     * </p>
     *
     * @param scores A list of scores (at least 2 elements long)
     * @return The higher integer average between the first and second half
     */
    public static int scoresAverage(int[] scores) {
        // TODO: Implement your solution here
        return 0;
    }
}


