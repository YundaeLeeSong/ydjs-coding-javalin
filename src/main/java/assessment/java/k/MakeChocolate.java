package assessment.java.k;

public class MakeChocolate {
    /**
     * <h3>Q2</h3>
     * <p>
     * Description:
     *     We want to make a package of `goal` kilos of chocolate. We have small
     *     bars
     *     (1 kilo each) and big bars (5 kilos each). Return the number of small
     *     bars
     *     to use, assuming we always use big bars before small bars. Return -1
     *     if it
     *     can't be done.
     * </p>
     * <p>
     * Examples:
     * <ul>
     *   <li>makeChocolate(4, 1, 9) -> 4</li>
     *   <li>makeChocolate(4, 1, 10) -> -1</li>
     *   <li>makeChocolate(4, 1, 7) -> 2</li>
     * </ul>
     * </p>
     *
     * @param small Number of small 1-kg bars available
     * @param big Number of big 5-kg bars available
     * @param goal Target weight in kilos
     * @return The number of small bars to use, or -1 if the goal cannot be met
     */
    public static int makeChocolate(int small, int big, int goal) {
        // TODO: Implement your solution here
        return -1;
    }
}


