package assessment.java.c;

public class SumDigits {
    /**
     * <h3>Q12</h3>
     * <p>
     * Description:
     *     Given a non-negative int n, return the sum of its digits recursively
     *     (no loops).
     *     Note that mod (%) by 10 yields the rightmost digit (e.g., 126 % 10 is
     *     6), while integer division (/)
     *     by 10 removes the rightmost digit (e.g., 126 / 10 is 12).
     * </p>
     * <p>
     * Examples:
     * <ul>
     *   <li>sumDigits(126) -> 9</li>
     *   <li>sumDigits(49) -> 13</li>
     *   <li>sumDigits(12) -> 3</li>
     * </ul>
     * </p>
     *
     * @param n A non-negative integer
     * @return The sum of the digits of n
     */
    public static int sumDigits(int n) {
        // TODO: Implement your solution here
        return 0;
    }
}
