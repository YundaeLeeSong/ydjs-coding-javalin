package assessment.java.j;

public class SplitArray {
    /**
     * <h3>Q14</h3>
     * <p>
     * Description:
     *     Given an array of ints, determine recursively if it is possible to
     *     divide the ints 
     *     into two groups, so that the sums of the two groups are the same.
     *     Every int must be 
     *     in one group or the other. A recursive helper method is used to try
     *     all possibilities.
     * </p>
     * <p>
     * Examples:
     * <ul>
     *   <li>splitArray([2,2]) -> true</li>
     *   <li>splitArray([2,3]) -> false</li>
     *   <li>splitArray([5,2,3]) -> true</li>
     * </ul>
     * </p>
     *
     * @param nums The input list of integers
     * @return true if it is possible to split the array into two groups with
     * equal sum, false otherwise
     */
    public static boolean splitArray(int[] nums) {
        // TODO: Implement your solution here
        return false;
    }
}


