package assessment.java.g;

public class XyBalance {
    /**
     * <h3>Q7</h3>
     * <p>
     * Description:
     *     We'll say that a string is xy-balanced if for all the 'x' characters
     *     in the string,
     *     there exists a 'y' character somewhere later in the string. One 'y'
     *     can balance multiple 'x's.
     *     Return True if the given string is xy-balanced.
     * </p>
     * <p>
     * Examples:
     * <ul>
     *   <li>xyBalance("aaxbby") -> true    # both x's have a y after them</li>
     *   <li>xyBalance("aaxbb") -> false    # x at pos 2 has no y after</li>
     *   <li>xyBalance("yaaxbb") -> false   # same, no y after the x's</li>
     * </ul>
     * </p>
     *
     * @param s The input string to check for xy-balance
     * @return true if the string is xy-balanced, false otherwise
     */
    public static boolean xyBalance(String s) {
        // TODO: Implement your solution here
        return false;
    }
}


