package assessment.java.g;

public class IsEverywhere {
    /**
     * <h3>Q4</h3>
     * <p>
     * Description:
     *     We'll say that a value is "everywhere" in a list if for every pair of
     *     adjacent elements
     *     in the list, at least one of the pair is that value. Return True if
     *     the given value is
     *     everywhere in the list. An empty list or a single-element list
     *     trivially satisfies this.
     * </p>
     * <p>
     * Examples:
     * <ul>
     *   <li>isEverywhere([1, 2, 1, 3], 1) -> true</li>
     *   <li>isEverywhere([1, 2, 1, 3], 2) -> false</li>
     *   <li>isEverywhere([1, 2, 1, 3, 4], 1) -> false</li>
     * </ul>
     * </p>
     *
     * @param nums The list of integers to check
     * @param val The value to verify is "everywhere"
     * @return true if `val` is everywhere in `nums`, false otherwise
     */
    public static boolean isEverywhere(int[] nums, int val) {
        // TODO: Implement your solution here
        return false;
    }
}
