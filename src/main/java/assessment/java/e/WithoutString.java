package assessment.java.e;

public class WithoutString {
    /**
     * <h3>Q5</h3>
     * <p>
     * Description:
     *     Given two strings, `base` and `remove`, return a version of the
     *     `base` string where all
     *     non-overlapping instances of the `remove` string have been removed.
     *     Removal is not case
     *     sensitive (e.g. removing "is" will remove "IS", "Is", etc.), but the
     *     remaining characters
     *     keep their original case. You may assume `remove` has length 1 or
     *     more.
     * </p>
     * <p>
     * Examples:
     * <ul>
     *   <li>withoutString("Hello there", "llo") -> "He there"</li>
     *   <li>withoutString("Hello there", "e") -> "Hllo thr"</li>
     *   <li>withoutString("Hello there", "x") -> "Hello there"</li>
     * </ul>
     * </p>
     *
     * @param base The original string
     * @param remove The substring to remove (case-insensitive)
     * @return The resulting string after all instances of `remove` have been
     * removed
     */
    public static String withoutString(String base, String remove) {
        // TODO: Implement your solution here
        return "";
    }
}
