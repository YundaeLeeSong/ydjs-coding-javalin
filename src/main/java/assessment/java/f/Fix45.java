package assessment.java.f;

public class Fix45 {
    /**
     * <h3>Q4</h3>
     * <p>
     * Description:
     *     Return a list containing exactly the same numbers as the given list,
     *     but rearranged so that every 4
     *     is immediately followed by a 5. Do not move the 4's themselves, but
     *     every other number may move.
     *     You may assume:
     *       - The list contains the same number of 4's and 5's.
     *       - Every 4 has a non-4 immediately after it.
     *       - 5's may appear anywhere in the original list.
     * </p>
     * <p>
     * Examples:
     * <ul>
     *   <li>fix45([5, 4, 9, 4, 9, 5]) -> [9, 4, 5, 4, 5, 9]</li>
     *   <li>fix45([1, 4, 1, 5]) -> [1, 4, 5, 1]</li>
     *   <li>fix45([1, 4, 1, 5, 5, 4, 1]) -> [1, 4, 5, 1, 1, 4, 5]</li>
     *   <li>fix45([4, 2, 2, 5]) -> [4, 5, 2, 2]</li>
     * </ul>
     * </p>
     *
     * @param nums The input list of integers containing matching numbers of 4's
     * and 5's
     * @return A new list where each 4 is immediately followed by a 5
     */
    public static int[] fix45(int[] nums) {
        // TODO: Implement your solution here
        return new int[0];
    }
}


