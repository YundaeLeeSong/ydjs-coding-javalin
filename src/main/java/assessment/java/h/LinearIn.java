package assessment.java.h;

public class LinearIn {
    /**
     * <h3>Q2</h3>
     * <p>
     * Description:
     *     Given two lists of integers sorted in increasing order, `outer` and
     *     `inner`,
     *     return True if all of the numbers in `inner` appear in `outer`. The
     *     optimal
     *     solution makes a single linear pass through both lists, taking
     *     advantage
     *     of their sorted order.
     * </p>
     * <p>
     * Examples:
     * <ul>
     *   <li>linearIn([1, 2, 4, 6], [2, 4]) -> true</li>
     *   <li>linearIn([1, 2, 4, 6], [2, 3, 4]) -> false</li>
     *   <li>linearIn([1, 2, 4, 4, 6], [2, 4]) -> true</li>
     * </ul>
     * </p>
     *
     * @param outer A sorted list of integers (the superset)
     * @param inner A sorted list of integers to check for membership in `outer`
     * @return true if every element of `inner` appears in `outer`, false
     * otherwise
     */
    public static boolean linearIn(int[] outer, int[] inner) {
        // TODO: Implement your solution here
        return false;
    }
}


