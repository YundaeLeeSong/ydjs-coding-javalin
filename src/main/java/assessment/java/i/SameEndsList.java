package assessment.java.i;

public class SameEndsList {
    /**
     * <h3>Q8</h3>
     * <p>
     * Description:
     *     Return True if the group of `length` numbers at the start and end of
     *     the list are the same.
     *     You may assume that 0 <= length <= len(nums).
     * </p>
     * <p>
     * Examples:
     * <ul>
     *   <li>sameEnds([5, 6, 45, 99, 13, 5, 6], 1) -> false</li>
     *   <li>sameEnds([5, 6, 45, 99, 13, 5, 6], 2) -> true</li>
     *   <li>sameEnds([5, 6, 45, 99, 13, 5, 6], 3) -> false</li>
     * </ul>
     * </p>
     *
     * @param nums The list of integers to check
     * @param length The number of elements to compare at both ends
     * @return true if the first `length` elements and the last `length`
     * elements are identical, false otherwise
     */
    public static boolean sameEnds(int[] nums, int length) {
        // TODO: Implement your solution here
        return false;
    }
}


